# Stvor public fixtures (site copy)

Published demo vectors served at `stvor.xyz/fixtures/`.

| File | What it is |
|---|---|
| `demo-deny-receipt.json` | Real offline-verifiable DENY receipt — agent committed 50000.00 USDC, execution attempted swapped destination |
| `keyset.json` | Test issuer keyset for offline verification |

Regenerate demo receipt: `bun run fixtures:demo`

Source of truth: [github.com/stvor-hq/core](https://github.com/stvor-hq/core)
